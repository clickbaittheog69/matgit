clc;
clear;
matrix = zeros(3,3);
locstore = [2 2];
matrix(locstore(1),locstore(2)) = 1;
north = locstore(1)-1;
south = locstore(1)+1;
east = locstore(2)+1;
west = locstore(2)-1;

for ii = 1:-2:-1
    for jj = 1:-2:-1
    if locstore(1) + jj >=3 || 

    

disp(matrix)