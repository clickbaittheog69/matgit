clc; clear;
% M10 Activity - Checkerboard
% create blank image
board = uint8(zeros(800,800, 1));

% create variable to denote black or white


% change pixels to white as needed
for ii = 1:8
    if mod(ii, 2) == 1 %for odd numbered squares
        
       
       

    

imshow(board)
